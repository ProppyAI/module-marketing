#!/usr/bin/env bash
set -euo pipefail
EVENT_DATA=$(cat)
JOB_ID=$(echo "$EVENT_DATA" | python3 -c "import json,sys; print(json.load(sys.stdin).get('entity_id','unknown'))")
echo "{\"outcome\": \"continue\", \"output\": \"Scheduled review request for job $JOB_ID\"}"
