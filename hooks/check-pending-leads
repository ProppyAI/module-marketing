#!/usr/bin/env bash
set -euo pipefail
EVENT_DATA=$(cat)
JOB_NAME=$(echo "$EVENT_DATA" | python3 -c "import json,sys; print(json.load(sys.stdin).get('data',{}).get('job','unknown'))")
echo "{\"outcome\": \"continue\", \"output\": \"Checked pending leads for $JOB_NAME\"}"
